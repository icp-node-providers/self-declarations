# Waived required documents for declarations backfilled from the retired wiki.
#
# The wiki did not always host a complete set of documents: some pages linked to
# a public company register instead of hosting a proof of identity, and a few
# pages carried no document at all. Those providers are recorded here so that
# CI reports the gap as a warning instead of a failure.
#
# This list is for the backfill only. Every new submission must supply both the
# self-declaration and a proof of identity; do not add entries for new
# providers. When a provider supplies the missing document, delete their line.
#
# Format, one waiver per line:
#   <provider-slug> <doc-type>   # reason
#
anonstake                          proof-of-identity  # public register linked in the provider README
aspire-properties                  proof-of-identity  # public register linked in the provider README
blockchain-development-labs        proof-of-identity  # public register linked in the provider README
coplus-limited                     proof-of-identity  # public register linked in the provider README
honeycomb-capital-pty-ltd          proof-of-identity  # no such document on the wiki page
infoobjects                        proof-of-identity  # public register linked in the provider README
krishna-enterprises                proof-of-identity  # combined into the self-declaration document
mb-patrankos-suvis                 proof-of-identity  # public register linked in the provider README
ml-solutions-ltd                   proof-of-identity  # public register linked in the provider README
novisystems-llc                    proof-of-identity  # public register linked in the provider README
pindar-technology-limited          proof-of-identity  # public register linked in the provider README
privoxy-solutions-llc              proof-of-identity  # public register linked in the provider README
rivram-inc                         proof-of-identity  # public register linked in the provider README
wancloud-limited                   proof-of-identity  # public register linked in the provider README
zondax-ag                          proof-of-identity  # no such document on the wiki page
